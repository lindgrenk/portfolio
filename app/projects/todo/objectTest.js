	// var listItem   = document.createElement('li'),
		// 	listText   = document.createElement('p'),
		// 	buttonType = ["Complete", "Change", "Delete"],
		// 	buttons    = {};

		// listText.innerHTML = inputField.value;
		// listItem.appendChild(listText);

		// for (var i = 0; i < buttonType.length; i++) {
		// 	var btn                = document.createElement('button');
		// 	buttons[buttonType[i]] = buttonType[i];
		// 	btn.innerHTML          = buttons[Object.keys(buttons)[i]];
		// 	btn.classList.add('button-' + buttonType[i]);
		// 	buttons[buttonType[i]] = btn;
		// 	listItem.appendChild(btn);
		// }

		// var listContent = {
		// 	text    : listText,
		// 	buttons : buttons
		// }
